language: java
jdk:
  - oraclejdk8

sudo: false

env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8

script: mvn cobertura:cobertura coveralls:cobertura